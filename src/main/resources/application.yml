server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xiaoshao/config-center
        basedir: config
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0
